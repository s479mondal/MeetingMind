package com.meetingmind.config;

/**
 * CORS configuration is handled in {@link CorsConfig}.
 * This class is intentionally left empty to avoid duplicate CORS mappings.
 *
 * @see CorsConfig
 */
public class WebConfig {
    // Intentionally empty — do not add WebMvcConfigurer implementations here.
    // All CORS rules live in CorsConfig.java.
}
